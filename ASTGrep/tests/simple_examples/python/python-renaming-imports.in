from base64 import b64encode, something as invoke

import base64 as xyz

import torch.nn as nn
from torch.nn.parallel import DistributedDataParallel

from subprocess import run as run
import matplotlib.pyplot as plt
import bitcoin.core.script as script

import numpy as np

nn()
torch.nn()
xyz.something("aaa")

def function_with_renamed_parameter(self, serial_id: int, script: str, sats: int) -> None:
    txout = CTxOut(sats, CScript(bytes.fromhex(script)))

bitcoin.core.script = aa()

def aa(self, other: str):
    script(other)

run("aaa")
np.array()
plt.plot()
script.something("aaaa")

invoke("aaaa")

def aaa():
    invoke("aaaa")

def script(other: str):
    pass

import RPi.GPIO as GPIO

def func():
    global GPIO
    if GPIO is None:
        pass
